// import Config from 'react-native-config'

export const ApiConfig = {
  // TODO: get the config and secret key from environment using react-native-config
  API_BASE_URL: 'https://maps.googleapis.com/maps/api/place/',
  API_TIME_OUT: 30000,
  GOOGLE_MAP_API_KEY: '', //I will give you google map api key via private message
}
